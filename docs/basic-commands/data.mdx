---
sidebar_position: 4
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Link from '@docusaurus/Link';
export const Highlight = ({children, color}) => (
  <span
    style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
    {children}
  </span>
);

# Data

The data command will read/override/merge data of or to blocks and entities that contain NBT Data.

:::note

These docs are still work in progress, please join the <Link to="https://discord.gg/6GeGaGjs97">Discord Server</Link> for any specific help you require.

:::
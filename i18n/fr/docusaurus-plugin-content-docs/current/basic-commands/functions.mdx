---
sidebar_position: 1
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Link from '@docusaurus/Link';
export const Highlight = ({children, color}) => (
  <span
    style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
    {children}
  </span>
);

# Fonction

La commande de fonction exécute n'importe quel fichier de sélection `.mcfunction` lorsqu'elle est appelée.

:::note

Ces documents sont toujours en cours de travail, veuillez rejoindre le <Link to="https://discord.datapackhub.net/">Serveur Discord</Link> pour toute aide spécifique dont vous avez besoin.

:::
---
sidebar_position: 4
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Link from '@docusaurus/Link';
export const Highlight = ({children, color}) => (
  <span
    style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
    {children}
  </span>
);

# Donnés

La commande data permet à l'utilisateur d'obtenir, de fusionner, de modifier et de supprimer les données NBT d'une entité de bloc, d'une entité ou d'un stockage NBT de commande.

:::note

Ces documents sont toujours en cours de travail, veuillez rejoindre le <Link to="https://discord.datapackhub.net/">Serveur Discord</Link> pour toute aide spécifique dont vous avez besoin.

:::